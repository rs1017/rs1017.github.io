---
layout: post
title: "Git 커밋 메시지 변환 및 검증 스킬"
date: 2026-01-21 09:00:00 +0900
categories: [Workflow]
tags:
  - git
  - 변환
  - 검증
  - 커밋메시지
  - 자동화
skill_path: /skills/git-커밋-메시지-변환-및-검증-스킬/
difficulty: intermediate
generated_by: claude-sonnet-4
image:
  path: /assets/img/posts/2026-01-21-git-커밋-메시지-변환-및-검증-스킬/thumbnail.jpg
  alt: Git 커밋 메시지 변환 및 검증 스킬 썸네일
---

```markdown
---
layout: post
title: "Git 커밋 메시지 변환 및 검증 스킬"
date: 2026-01-21 09:00:00 +0900
categories: [AI, Tech]
tags: [git, 변환, 검증, 커밋메시지, 자동화]
description: "Git 커밋 메시지를 다양한 컨벤션(Conventional Commits, Gitmoji 등)으로 변환하고, 규칙 준수 여부를 자동 검증하는 스킬"
image: /assets/img/posts/2026-01-21-git-커밋-메시지-변환-및-검증-스킬/header.jpg
---

Git 커밋 메시지, 여러분의 프로젝트에서는 어떤 규칙을 따르고 계신가요? 팀마다 선호하는 컨벤션이 다르고, 프로젝트가 바뀔 때마다 새로운 규칙에 적응해야 하는 건 생각보다 번거로운 일입니다. 오늘은 Claude API를 활용해 커밋 메시지를 자동으로 변환하고 검증하는 워크플로우 스킬을 소개합니다.

## 왜 커밋 메시지 컨벤션이 중요할까요?

좋은 커밋 메시지는 프로젝트의 역사를 기록하는 중요한 문서입니다. Conventional Commits, Gitmoji, Angular 등 다양한 컨벤션이 존재하는 이유는 각각의 장점이 있기 때문이죠.

- **Conventional Commits**: `feat:`, `fix:`, `docs:` 등 타입 기반으로 명확한 분류
- **Gitmoji**: 😎, 🐛, 📝 등 이모지로 시각적 구분
- **Angular**: 엄격한 구조로 자동화된 CHANGELOG 생성 가능
- **Semantic**: 버전 관리와 직접 연동

[IMAGE_DESC: 다양한 Git 커밋 메시지 컨벤션들을 나타내는 비교 다이어그램. Conventional Commits의 'feat: 새 기능', Gitmoji의 '✨ 새 기능', Angular의 'feat(scope): 새 기능' 등이 나란히 표시되어 있음]

문제는 레거시 프로젝트를 인수인계 받았을 때, 또는 팀의 컨벤션이 바뀌었을 때입니다. 수백 개의 커밋 메시지를 일일이 수정할 수는 없죠. 이럴 때 Claude의 자연어 이해 능력을 활용하면 어떨까요?

## 스킬 핵심 기능

이 스킬은 크게 세 가지 기능을 제공합니다:

### 1. 다중 컨벤션 변환

원본 커밋 메시지의 의도를 파악해 목표 컨벤션으로 자동 변환합니다. Claude는 단순 패턴 매칭이 아니라 문맥을 이해하기 때문에 "사용자 로그인 버그 수정"이라는 평범한 메시지도 `fix(auth): resolve login validation error`로 정확하게 변환할 수 있습니다.

```python
from git_commit_skill import GitCommitMessageSkill, TransformRequest, ConventionType

skill = GitCommitMessageSkill()

# 평범한 한국어 메시지를 Conventional Commits로
request = TransformRequest(
    message="사용자 로그인 버그 수정",
    target_convention=ConventionType.CONVENTIONAL,
    language="ko"
)

result = skill.transform_message(request)
print(result.transformed_message)
# 출력: fix(auth): 로그인 검증 오류 해결
```

[IMAGE_DESC: 커밋 메시지 변환 프로세스를 보여주는 플로우차트. '원본 메시지' → 'Claude API 분석' → '컨벤션 규칙 적용' → '변환된 메시지' 순서로 화살표가 연결됨. Claude 로고가 중앙 분석 단계에 표시됨]

### 2. 실시간 검증

팀에서 정한 커밋 메시지 규칙을 위반하면 즉시 알려줍니다. 단순히 "틀렸다"고만 하는 게 아니라, 어떤 부분이 문제인지, 어떻게 고치면 좋을지 구체적인 피드백을 제공합니다.

```python
from git_commit_skill import ValidationRule

# 팀 규칙 정의: Conventional Commits, 스코프 필수, 72자 제한
rule = ValidationRule(
    convention=ConventionType.CONVENTIONAL,
    max_length=72,
    require_scope=True,
    allowed_types=["feat", "fix", "docs", "refactor", "test"]
)

# 검증 실행
message = "added new feature for user dashboard"
validation = skill.validate_message(message, rule)

if not validation.is_valid:
    print("❌ 검증 실패:")
    for error in validation.errors:
        print(f"  - {error}")
    print(f"\n✅ 제안: {validation.corrected_message}")
```

실행 결과:
```
❌ 검증 실패:
  - 타입이 허용된 목록에 없습니다: 'added'
  - 스코프가 필수이지만 누락되었습니다
  - Conventional Commits 형식이 아닙니다

✅ 제안: feat(dashboard): add new feature for user dashboard
```

### 3. 배치 처리

여러 커밋 메시지를 한 번에 처리할 수 있습니다. 이건 PR 리뷰나 레거시 프로젝트 정리할 때 정말 유용합니다.

```python
# Git 히스토리에서 최근 10개 커밋 메시지 가져오기
import subprocess

result = subprocess.run(
    ["git", "log", "--format=%s", "-10"],
    capture_output=True,
    text=True
)
messages = result.stdout.strip().split("\n")

# 일괄 변환
requests = [
    TransformRequest(msg, target_convention=ConventionType.GITMOJI)
    for msg in messages
]

results = skill.batch_transform(requests)
for original, transformed in zip(messages, results):
    print(f"{original}\n  → {transformed.transformed_message}\n")
```

[IMAGE_DESC: 배치 처리 결과를 보여주는 터미널 스크린샷. 왼쪽에는 'Before: Update user profile', 'Fix login bug' 등의 원본 메시지들이, 오른쪽에는 '✨ Update user profile', '🐛 Fix login validation' 등 Gitmoji가 적용된 변환 메시지들이 나란히 표시됨]

## 실전 활용: Pre-commit Hook 통합

이 스킬의 진가는 Git Hook과 통합했을 때 발휘됩니다. `.git/hooks/commit-msg` 파일에 스크립트를 추가하면 커밋할 때마다 자동으로 검증되고, 규칙에 맞지 않으면 커밋이 차단됩니다.

```python
#!/usr/bin/env python3
# .git/hooks/commit-msg

import sys
from git_commit_skill import GitCommitMessageSkill, ValidationRule, ConventionType

def main():
    commit_msg_file = sys.argv[1]
    
    with open(commit_msg_file, 'r', encoding='utf-8') as f:
        message = f.read().strip()
    
    # 팀 규칙
    rule = ValidationRule(
        convention=ConventionType.CONVENTIONAL,
        max_length=72,
        require_scope=True
    )
    
    skill = GitCommitMessageSkill()
    validation = skill.validate_message(message, rule)
    
    if not validation.is_valid:
        print("🚫 커밋 메시지가 팀 규칙을 위반합니다:\n")
        for error in validation.errors:
            print(f"  ❌ {error}")
        print(f"\n💡 제안된 메시지:")
        print(f"  {validation.corrected_message}\n")
        
        # 자동 수정 옵션
        response = input("자동으로 수정하시겠습니까? (y/N): ")
        if response.lower() == 'y':
            with open(commit_msg_file, 'w', encoding='utf-8') as f:
                f.write(validation.corrected_message)
            print("✅ 커밋 메시지가 수정되었습니다.")
            return 0
        else:
            return 1
    
    print("✅ 커밋 메시지 검증 통과")
    return 0

if __name__ == "__main__":
    sys.exit(main())
```

이제 규칙에 맞지 않는 커밋을 시도하면:

```bash
$ git commit -m "fixed bug"
🚫 커밋 메시지가 팀 규칙을 위반합니다:

  ❌ 타입이 허용된 형식이 아닙니다
  ❌ 스코프가 필수이지만 누락되었습니다

💡 제안된 메시지:
  fix(core): resolve bug

자동으로 수정하시겠습니까? (y/N): y
✅ 커밋 메시지가 수정되었습니다.
[main a1b2c3d] fix(core): resolve bug
```

## 고급 활용: CI/CD 파이프라인 통합

GitHub Actions나 GitLab CI에서 PR의 모든 커밋 메시지를 자동으로 검증할 수 있습니다.

```yaml
# .github/workflows/commit-lint.yml
name: Commit Message Lint

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          pip install anthropic
          # 스킬 코드 다운로드
          curl -o git_commit_skill.py https://your-domain.com/skills/git-commit-skill/example.py
      
      - name: Validate commit messages
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
        run: |
          python - <<EOF
          import subprocess
          from git_commit_skill import GitCommitMessageSkill, ValidationRule, ConventionType
          
          # PR의 모든 커밋 메시지 가져오기
          result = subprocess.run(
              ["git", "log", "--format=%s", "origin/main..HEAD"],
              capture_output=True,
              text=True
          )
          messages = result.stdout.strip().split("\n")
          
          rule = ValidationRule(
              convention=ConventionType.CONVENTIONAL,
              require_scope=True
          )
          
          skill = GitCommitMessageSkill()
          failed = []
          
          for msg in messages:
              validation = skill.validate_message(msg, rule)
              if not validation.is_valid:
                  failed.append((msg, validation))
          
          if failed:
              print("❌ 다음 커밋 메시지들이 규칙을 위반합니다:\n")
              for msg, val in failed:
                  print(f"커밋: {msg}")
                  for error in val.errors:
                      print(f"  - {error}")
                  print(f"제안: {val.corrected_message}\n")
              exit(1)
          
          print("✅ 모든 커밋 메시지가 규칙을 준수합니다.")
          EOF
```

## 실제 사용 예시

### 케이스 1: 오픈소스 프로젝트 기여

오픈소스 프로젝트마다 커밋 메시지 컨벤션이 다릅니다. Linux 커널은 상세한 본문을 요구하고, React는 타입 분류를 선호하죠. 이 스킬로 각 프로젝트에 맞게 메시지를 변환할 수 있습니다.

```python
# Angular 프로젝트 기여용 변환
request = TransformRequest(
    message="Added TypeScript support for new component",
    target_convention=ConventionType.ANGULAR,
    language="en"
)

result = skill.transform_message(request)
# 출력: feat(component): add TypeScript support
```

### 케이스 2: 다국적 팀 협업

영어로 커밋했지만 한국 팀원들이 히스토리를 볼 때는 한국어가 편할 수 있습니다. 언어 변환도 지원합니다.

```python
request = TransformRequest(
    message="fix(auth): resolve OAuth token expiration issue",
    target_convention=ConventionType.CONVENTIONAL,
    language="ko"
)

result = skill.transform_message(request)
# 출력: fix(auth): OAuth 토큰 만료 문제 해결
```

### 케이스 3: 레거시 프로젝트 정리

5년 된 프로젝트의 커밋 메시지를 현대적인 컨벤션으로 정리하고 싶을 때:

```python
import subprocess

# 전체 커밋 히스토리 변환 (주의: 큰 프로젝트는 시간 소요)
result = subprocess.run(
    ["git", "log", "--format=%H|||%s", "--all"],
    capture_output=True,
    text=True
)

commits = [line.split("|||") for line in result.stdout.strip().split("\n")]

# 변환 결과를 파일로 저장
with open("commit_migration.txt", "w", encoding="utf-8") as f:
    for commit_hash, message in commits:
        request = TransformRequest(
            message=message,
            target_convention=ConventionType.CONVENTIONAL
        )
        transformed = skill.transform_message(request)
        f.write(f"{commit_hash}: {message}\n")
        f.write(f"  → {transformed.transformed_message}\n\n")

print("변환 결과가 commit_migration.txt에 저장되었습니다.")
```

## 성능 및 비용 고려사항

Claude API를 사용하므로 호출당 비용이 발생합니다. 효율적으로 사용하는 팁:

1. **배치 처리 활용**: 개별 호출보다 `batch_transform()` 사용
2. **캐싱 전략**: 동일한 메시지는 결과 캐싱 (스킬에 내장)
3. **검증 우선**: 간단한 정규식 검증 후 실패 시에만 Claude 호출
4. **모델 선택**: 간단한 변환은 Claude 3.5 Haiku도 충분

```python
# 비용 절감 예시: 두 단계 검증
import re

def quick_validate(message: str) -> bool:
    """정규식으로 빠른 1차 검증"""
    pattern = r'^(feat|fix|docs|style|refactor|test|chore)(\(.+\))?: .+'
    return bool(re.match(pattern, message))

# 1차 통과 시 Claude 호출 생략
if quick_validate(commit_message):
    print("✅ 빠른 검증 통과")
else:
    # 1차 실패 시에만 Claude로 정밀 검증 및 제안
    validation = skill.validate_message(commit_message, rule)
```

## 직접 사용해보기

이 스킬의 전체 코드는 [여기](/skills/git-커밋-메시지-변환-및-검증-스킬/)에서 확인할 수 있습니다. 

시작하기:

```bash
# 스킬 다운로드
curl -o git_commit_skill.py https://aiskill.factory/skills/git-커밋-메시지-변환-및-검증-스킬/example.py

# 의존성 설치
pip install anthropic>=0.35.0

# API 키 설정
export ANTHROPIC_API_KEY="your-api-key"

# 실행
python git_commit_skill.py
```

## 마무리하며

커밋 메시지 컨벤션은 코드만큼이나 중요한 프로젝트의 자산입니다. 하지만 사람이 일일이 규칙을 외우고 지키기는 쉽지 않죠. Claude의 자연어 이해 능력을 활용하면 이 과정을 자동화하면서도 유연하게 대응할 수 있습니다.

여러분의 팀은 어떤 커밋 메시지 컨벤션을 사용하시나요? 이 스킬을 활용해 팀의 생산성을 한 단계 높여보세요. 질문이나 개선 아이디어가 있다면 댓글로 공유해주시면 감사하겠습니다! 🚀
```