---
layout: post
title: "API 데이터 기반 PDF 캘린더 자동 생성 스킬"
date: 2026-01-21 00:00:00 +0900
categories: [Skill]
tags: [API, PDF, 캘린더, 자동화, 데이터-시각화]
image:
  path: /assets/img/posts/2026-01-21-api-데이터-기반-pdf-캘린더-자동-생성-스킬/header.jpg
  alt: "API 데이터 기반 PDF 캘린더 자동 생성 스킬"
---

## 🎯 개요

매달 반복되는 일정 정리, 손으로 캘린더에 일일이 기록하는 작업... 지겹지 않으신가요? 외부 API에서 일정 데이터를 가져와 Claude API의 인텔리전스를 더해 자동으로 PDF 캘린더를 만들어주는 스킬을 소개합니다.

Google Calendar, Outlook, 사내 시스템 등 어떤 API든 연결하면 일정을 자동으로 분석하고, 카테고리별로 색상을 입혀 보기 좋은 PDF 캘린더로 변환해줍니다. 회사 프로젝트 일정, 팀 미팅, 개인 일정까지 한 번에 시각화해보세요!

[IMAGE_DESC: API 데이터를 수집하여 Claude가 분석하고 아름다운 PDF 캘린더로 변환하는 전체 프로세스를 보여주는 다이어그램. 왼쪽에 Google Calendar, Outlook 등 다양한 API 아이콘, 중앙에 Claude AI 로고, 오른쪽에 컬러풀한 월간 캘린더 PDF가 표시됨]

### 이런 분들께 추천합니다

- 여러 캘린더 서비스를 사용하는 프로젝트 매니저
- 팀원들의 일정을 통합 관리해야 하는 리더
- 고객사에 월간 일정표를 제출해야 하는 담당자
- 개인 시간 관리를 시각화하고 싶은 분

---

## 📁 폴더 구조

이 스킬을 사용하기 위한 권장 프로젝트 구조입니다:

```
calendar-generator/
├── calendar_generator.py       # 메인 스크립트
├── config.json                 # API 키 및 설정
├── templates/
│   ├── monthly.json           # 월간 캘린더 템플릿
│   └── weekly.json            # 주간 캘린더 템플릿
├── output/                    # 생성된 PDF 저장 폴더
│   ├── 2026-01-monthly.pdf
│   └── 2026-02-monthly.pdf
└── requirements.txt
```

---

## 🔄 동작 흐름

스킬이 어떻게 동작하는지 단계별로 살펴보겠습니다:

```
┌──────────────────┐
│  1. API 데이터   │
│     수집         │  ← Google Calendar, Outlook, Custom API
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  2. Claude 분석  │
│  - 카테고리 분류 │  ← Claude API: 일정 중요도 판단, 색상 할당
│  - 우선순위 지정 │
│  - 중복 제거     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  3. HTML 구조화  │
│  - 월간/주간 뷰  │  ← ReportLab으로 테이블 구조 생성
│  - 컬러 테마     │
└────────┬─────────┘
         │
         ▼
┌──────────────────┐
│  4. PDF 생성     │
│  - A4/Letter     │  ← 최종 PDF 파일 출력
│  - 한글 폰트     │
└──────────────────┘
```

[IMAGE_DESC: 4단계 파이프라인을 보여주는 플로우차트. 각 단계마다 아이콘과 함께 입력/출력 데이터 형식이 명확하게 표시되어 있음. API 엔드포인트에서 시작하여 최종 PDF 파일 아이콘으로 끝나는 흐름]

---

## 🛠️ 구현

### Step 1: 환경 설정

먼저 필요한 패키지를 설치합니다:

```bash
pip install anthropic reportlab requests python-dateutil pytz
```

환경 변수에 API 키를 설정하세요:

```bash
export ANTHROPIC_API_KEY="your-api-key-here"
export GOOGLE_CALENDAR_API_KEY="your-google-api-key"  # 선택 사항
```

### Step 2: 설정 파일 작성

`config.json` 파일을 만들어 캘린더 설정을 정의합니다:

```json
{
  "api_endpoints": {
    "google_calendar": "https://www.googleapis.com/calendar/v3/calendars/primary/events",
    "custom_api": "https://your-company.com/api/events"
  },
  "calendar_settings": {
    "page_size": "A4",
    "language": "ko",
    "timezone": "Asia/Seoul",
    "color_scheme": "default"
  },
  "output": {
    "directory": "./output",
    "filename_pattern": "{year}-{month:02d}-calendar.pdf"
  }
}
```

### Step 3: 메인 스크립트 작성

이제 실제로 동작하는 캘린더 생성기를 만들어봅시다. 전체 코드는 아래와 같습니다:

```python
#!/usr/bin/env python3
"""
API 데이터 기반 PDF 캘린더 자동 생성 스킬
"""

import os
import sys
import json
import calendar
from datetime import datetime, timedelta
from typing import List, Dict, Optional
from dataclasses import dataclass
from enum import Enum

from anthropic import Anthropic
from reportlab.lib.pagesizes import A4
from reportlab.lib import colors
from reportlab.lib.units import inch
from reportlab.platypus import SimpleDocTemplate, Table, TableStyle, Paragraph, Spacer
from reportlab.lib.styles import getSampleStyleSheet
from reportlab.pdfbase import pdfmetrics
from reportlab.pdfbase.ttfonts import TTFont
import requests


class EventCategory(Enum):
    """이벤트 카테고리"""
    WORK = ("업무", colors.HexColor("#4A90E2"))
    MEETING = ("회의", colors.HexColor("#E94B3C"))
    PERSONAL = ("개인", colors.HexColor("#50C878"))
    DEADLINE = ("마감", colors.HexColor("#F39C12"))
    EVENT = ("행사", colors.HexColor("#9B59B6"))
    OTHER = ("기타", colors.HexColor("#95A5A6"))


@dataclass
class CalendarEvent:
    """캘린더 이벤트 데이터 클래스"""
    title: str
    date: str
    time: Optional[str] = None
    description: Optional[str] = None
    category: str = "기타"
    priority: int = 1


class CalendarGenerator:
    """PDF 캘린더 생성기"""
    
    def __init__(self, api_key: str, config_path: str = "config.json"):
        self.client = Anthropic(api_key=api_key)
        self.config = self._load_config(config_path)
        
    def _load_config(self, path: str) -> Dict:
        """설정 파일 로드"""
        if os.path.exists(path):
            with open(path, 'r', encoding='utf-8') as f:
                return json.load(f)
        return {}
    
    def fetch_events_from_api(self, api_url: str, year: int, month: int) -> List[Dict]:
        """API에서 이벤트 데이터 가져오기"""
        print(f"📡 API에서 {year}년 {month}월 일정을 가져오는 중...")
        
        # 실제 API 호출 (예시)
        params = {
            "timeMin": f"{year}-{month:02d}-01T00:00:00Z",
            "timeMax": f"{year}-{month+1 if month < 12 else 1:02d}-01T00:00:00Z"
        }
        
        try:
            response = requests.get(api_url, params=params, timeout=10)
            response.raise_for_status()
            return response.json().get('items', [])
        except Exception as e:
            print(f"⚠️ API 호출 실패: {e}")
            # 데모용 샘플 데이터 반환
            return self._get_sample_events(year, month)
    
    def _get_sample_events(self, year: int, month: int) -> List[Dict]:
        """샘플 이벤트 데이터"""
        return [
            {
                "title": "프로젝트 킥오프 미팅",
                "date": f"{year}-{month:02d}-05",
                "time": "14:00",
                "description": "Q1 신규 프로젝트 시작"
            },
            {
                "title": "코드 리뷰",
                "date": f"{year}-{month:02d}-12",
                "time": "10:00",
                "description": "주간 코드 리뷰 세션"
            },
            {
                "title": "릴리스 마감",
                "date": f"{year}-{month:02d}-28",
                "time": "18:00",
                "description": "v2.0 배포 마감일"
            }
        ]
    
    def analyze_events_with_claude(self, events: List[Dict]) -> List[CalendarEvent]:
        """Claude API로 이벤트 분석 및 분류"""
        print("🤖 Claude AI가 일정을 분석하는 중...")
        
        prompt = f"""다음 일정 데이터를 분석하여 각 이벤트에 적절한 카테고리와 우선순위를 할당해주세요.

이벤트 데이터:
{json.dumps(events, ensure_ascii=False, indent=2)}

카테고리 옵션: 업무, 회의, 개인, 마감, 행사, 기타
우선순위: 1(낮음), 2(보통), 3(높음)

JSON 배열 형식으로 응답해주세요:
[
  {{
    "title": "...",
    "date": "YYYY-MM-DD",
    "time": "HH:MM",
    "category": "...",
    "priority": 1-3,
    "description": "..."
  }}
]
"""
        
        try:
            message = self.client.messages.create(
                model="claude-3-5-sonnet-20241022",
                max_tokens=2000,
                messages=[{"role": "user", "content": prompt}]
            )
            
            response_text = message.content[0].text
            analyzed_events = json.loads(response_text)
            
            return [CalendarEvent(**event) for event in analyzed_events]
            
        except Exception as e:
            print(f"⚠️ Claude 분석 실패, 기본 분류 사용: {e}")
            return [CalendarEvent(**event) for event in events]
    
    def generate_monthly_pdf(self, year: int, month: int, events: List[CalendarEvent], output_path: str):
        """월간 캘린더 PDF 생성"""
        print(f"📄 PDF 캘린더 생성 중: {output_path}")
        
        doc = SimpleDocTemplate(output_path, pagesize=A4)
        story = []
        styles = getSampleStyleSheet()
        
        # 제목
        title = Paragraph(f"<b>{year}년 {month}월 캘린더</b>", styles['Title'])
        story.append(title)
        story.append(Spacer(1, 0.3*inch))
        
        # 캘린더 테이블 생성
        cal = calendar.monthcalendar(year, month)
        table_data = [['월', '화', '수', '목', '금', '토', '일']]
        
        # 날짜별 이벤트 매핑
        events_by_date = {}
        for event in events:
            day = int(event.date.split('-')[2])
            if day not in events_by_date:
                events_by_date[day] = []
            events_by_date[day].append(event)
        
        # 주별 행 생성
        for week in cal:
            week_row = []
            for day in week:
                if day == 0:
                    week_row.append('')
                else:
                    cell_content = f"{day}\n"
                    if day in events_by_date:
                        for evt in events_by_date[day][:2]:  # 최대 2개만 표시
                            cell_content += f"• {evt.title[:10]}\n"
                    week_row.append(cell_content)
            table_data.append(week_row)
        
        # 테이블 스타일 적용
        table = Table(table_data, colWidths=[1.1*inch]*7, rowHeights=[0.4*inch] + [1*inch]*len(cal))
        table.setStyle(TableStyle([
            ('BACKGROUND', (0, 0), (-1, 0), colors.grey),
            ('TEXTCOLOR', (0, 0), (-1, 0), colors.whitesmoke),
            ('ALIGN', (0, 0), (-1, -1), 'CENTER'),
            ('VALIGN', (0, 0), (-1, -1), 'TOP'),
            ('FONTNAME', (0, 0), (-1, 0), 'Helvetica-Bold'),
            ('FONTSIZE', (0, 0), (-1, 0), 12),
            ('FONTSIZE', (0, 1), (-1, -1), 9),
            ('GRID', (0, 0), (-1, -1), 0.5, colors.black),
            ('BACKGROUND', (5, 1), (6, -1), colors.HexColor('#FFF8DC'))  # 주말 배경색
        ]))
        
        story.append(table)
        story.append(Spacer(1, 0.3*inch))
        
        # 이벤트 목록
        story.append(Paragraph("<b>📅 전체 일정</b>", styles['Heading2']))
        for event in sorted(events, key=lambda e: e.date):
            category_color = self._get_category_color(event.category)
            event_text = f"<font color='{category_color}'>●</font> <b>{event.date}</b> {event.time or ''} - {event.title}"
            if event.description:
                event_text += f" ({event.description})"
            story.append(Paragraph(event_text, styles['Normal']))
        
        doc.build(story)
        print(f"✅ 캘린더 생성 완료: {output_path}")
    
    def _get_category_color(self, category: str) -> str:
        """카테고리별 색상 반환"""
        color_map = {
            "업무": "#4A90E2",
            "회의": "#E94B3C",
            "개인": "#50C878",
            "마감": "#F39C12",
            "행사": "#9B59B6",
            "기타": "#95A5A6"
        }
        return color_map.get(category, "#95A5A6")
    
    def run(self, year: int, month: int):
        """전체 프로세스 실행"""
        print(f"\n🚀 {year}년 {month}월 캘린더 생성 시작\n")
        
        # 1. API 데이터 수집
        api_url = self.config.get('api_endpoints', {}).get('custom_api', '')
        raw_events = self.fetch_events_from_api(api_url, year, month)
        
        # 2. Claude 분석
        analyzed_events = self.analyze_events_with_claude(raw_events)
        
        # 3. PDF 생성
        output_dir = self.config.get('output', {}).get('directory', './output')
        os.makedirs(output_dir, exist_ok=True)
        
        output_file = f"{output_dir}/{year}-{month:02d}-calendar.pdf"
        self.generate_monthly_pdf(year, month, analyzed_events, output_file)
        
        print(f"\n✨ 완료! 생성된 파일: {output_file}\n")


def main():
    """메인 함수"""
    api_key = os.getenv('ANTHROPIC_API_KEY')
    if not api_key:
        print("❌ ANTHROPIC_API_KEY 환경 변수를 설정해주세요.")
        sys.exit(1)
    
    # 현재 월 캘린더 생성
    now = datetime.now()
    
    generator = CalendarGenerator(api_key)
    generator.run(now.year, now.month)


if __name__ == "__main__":
    main()
```

[IMAGE_DESC: 생성된 PDF 캘린더의 실제 스크린샷. A4 용지에 월간 뷰로 표시되어 있으며, 각 날짜 셀에 컬러 점으로 구분된 이벤트들이 표시됨. 하단에는 전체 일정 목록이 카테고리별 색상과 함께 나열되어 있음]

### Step 4: 실행 및 검증

스크립트를 실행해봅시다:

```bash
python calendar_generator.py
```

정상적으로 실행되면 다음과 같은 출력을 볼 수 있습니다:

```
🚀 2026년 1월 캘린더 생성 시작

📡 API에서 2026년 1월 일정을 가져오는 중...
🤖 Claude AI가 일정을 분석하는 중...
📄 PDF 캘린더 생성 중: ./output/2026-01-calendar.pdf
✅ 캘린더 생성 완료: ./output/2026-01-calendar.pdf

✨ 완료! 생성된 파일: ./output/2026-01-calendar.pdf
```

---

## 💡 고급 활용

### 1. Google Calendar 연동

실제 Google Calendar API와 연동하려면:

```python
def fetch_google_calendar_events(self, calendar_id: str, year: int, month: int):
    """Google Calendar API 연동"""
    from google.oauth2.credentials import Credentials
    from googleapiclient.discovery import build
    
    creds = Credentials.from_authorized_user_file('token.json')
    service = build('calendar', 'v3', credentials=creds)
    
    time_min = f"{year}-{month:02d}-01T00:00:00Z"
    time_max = f"{year}-{month+1:02d}-01T00:00:00Z"
    
    events_result = service.events().list(
        calendarId=calendar_id,
        timeMin=time_min,
        timeMax=time_max,
        singleEvents=True,
        orderBy='startTime'
    ).execute()
    
    return events_result.get('items', [])
```

### 2. 주간 캘린더 생성

월간 뷰 대신 주간 상세 뷰를 원한다면:

```python
def generate_weekly_pdf(self, year: int, week_number: int, events: List[CalendarEvent], output_path: str):
    """주간 캘린더 PDF 생성 (시간별 타임 슬롯)"""
    # 07:00 ~ 22:00 시간대별 슬롯 생성
    # 각 요일별 컬럼에 이벤트 배치
    # 더 상세한 구현은 생략
    pass
```

### 3. 다중 캘린더 병합

여러 캘린더를 한 번에 통합하려면:

```python
def merge_multiple_calendars(self, calendar_sources: List[str]):
    """여러 캘린더 소스 병합"""
    all_events = []
    
    for source in calendar_sources:
        events = self.fetch_events_from_api(source, year, month)
        all_events.extend(events)
    
    # Claude로 중복 제거 및 병합
    return self.analyze_events_with_claude(all_events)
```

---

## 📎 첨부 파일

이 스킬의 전체 코드와 설정 파일은 다음 경로에서 확인할 수 있습니다:

> 📎 **전체 스킬 코드**: [api-데이터-기반-pdf-캘린더-자동-생성-스킬](/skills/api-데이터-기반-pdf-캘린더-자동-생성-스킬/)

---

## ✅ 실행 결과

이 스킬을 사용하면 다음과 같은 결과물을 얻을 수 있습니다:

1. **자동 분류된 일정**: Claude가 업무, 회의, 마감 등으로 자동 분류
2. **시각적 캘린더**: 카테고리별 색상으로 한눈에 일정 파악
3. **인쇄 가능한 PDF**: A4 용지에 최적화된 레이아웃
4. **다국어 지원**: 한국어, 영어 등 원하는 언어로 생성

### 활용 사례

- **프로젝트 관리**: 팀 전체의 마일스톤을 월간 캘린더로 시각화
- **회의 일정 공유**: 월초에 팀원들에게 회의 일정 PDF 배포
- **개인 시간 관리**: 업무/개인 일정을 통합하여 밸런스 확인
- **고객 리포팅**: 프로젝트 진행 상황을 캘린더 형태로 보고

---

## 🔗 관련 스킬

이 스킬과 함께 사용하면 좋은 다른 스킬들:

- **API 통합 스킬**: 다양한 외부 API 연동 방법
- **자동화 워크플로우**: 매월 자동으로 캘린더 생성 스케줄링
- **이메일 자동 발송**: 생성된 PDF를 팀원에게 자동 전송

---

## 🎓 마무리

API 데이터를 활용하여 맞춤형 PDF 캘린더를 자동으로 생성하는 방법을 알아보았습니다. Claude API의 인텔리전스를 더하면 단순 데이터 나열이 아닌, 의미 있는 분류와 우선순위가 적용된 스마트한 캘린더를 만들 수 있습니다.

이제 매달 반복되는 일정 정리 작업은 스크립트에 맡기고, 더 중요한 일에 집중해보세요! 🚀