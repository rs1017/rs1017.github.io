---
layout: post
title: "PDF - PDF 처리 종합 툴킷 스킬"
date: 2026-01-23 10:08:00 +0900
categories: [AI, Skill]
tags: [claude-code, skill, pdf, document, extraction]
---

## 개요

PDF는 텍스트/테이블 추출, 새 PDF 생성, 문서 병합/분할, 폼 처리를 위한 종합 PDF 조작 툴킷입니다.

## 빠른 시작

```python
from pypdf import PdfReader, PdfWriter

# PDF 읽기
reader = PdfReader("document.pdf")
print(f"Pages: {len(reader.pages)}")

# 텍스트 추출
text = ""
for page in reader.pages:
    text += page.extract_text()
```

## 주요 라이브러리

| 라이브러리 | 용도 |
|-----------|------|
| pypdf | 기본 작업 (병합, 분할, 회전) |
| pdfplumber | 텍스트 및 테이블 추출 |
| reportlab | PDF 생성 |
| pytesseract | 스캔 PDF OCR |

## 일반 작업 예시

### PDF 병합

```python
writer = PdfWriter()
for pdf_file in ["doc1.pdf", "doc2.pdf"]:
    reader = PdfReader(pdf_file)
    for page in reader.pages:
        writer.add_page(page)

with open("merged.pdf", "wb") as output:
    writer.write(output)
```

### 테이블 추출

```python
import pdfplumber

with pdfplumber.open("document.pdf") as pdf:
    for page in pdf.pages:
        tables = page.extract_tables()
        for table in tables:
            print(table)
```

## 커맨드라인 도구

```bash
# 텍스트 추출
pdftotext input.pdf output.txt

# PDF 병합
qpdf --empty --pages file1.pdf file2.pdf -- merged.pdf

# 페이지 분할
qpdf input.pdf --pages . 1-5 -- pages1-5.pdf
```

## 빠른 참조

| 작업 | 도구 |
|------|------|
| PDF 병합 | pypdf |
| 텍스트 추출 | pdfplumber |
| 테이블 추출 | pdfplumber |
| PDF 생성 | reportlab |
| 폼 채우기 | forms.md 참조 |

## 첨부 파일

> [pdf SKILL.md](/assets/downloads/skills/pdf/SKILL.md)

## 관련 스킬

- [docx](/posts/docx/) - Word 문서 작업
- [pptx](/posts/pptx/) - 프레젠테이션 작업
- [xlsx](/posts/xlsx/) - 스프레드시트 작업
