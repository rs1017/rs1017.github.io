---
name: hook-name
type: PreToolUse
matcher: Bash
description: 훅 설명
---

# Hook Name

## 🎯 목적

이 훅의 목적을 설명합니다.

## 🔧 설정

`.claude/settings.json`에 등록:

```json
{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": ["python .claude/hooks/hook_name.py"]
      }
    ]
  }
}
```

## 🔄 동작 흐름

```
┌──────────────┐
│ Tool 호출    │
└──────┬───────┘
       │
       ▼
┌──────────────┐     ┌──────────────┐
│ Hook 실행    │────▶│ 조건 확인    │
└──────────────┘     └──────┬───────┘
                            │
              ┌─────────────┼─────────────┐
              ▼             ▼             ▼
        ┌──────────┐  ┌──────────┐  ┌──────────┐
        │ continue │  │  block   │  │  modify  │
        └──────────┘  └──────────┘  └──────────┘
```

## 📋 Hook Types

| Type | 설명 | 사용 시점 |
|------|------|----------|
| `PreToolUse` | 도구 실행 전 | 실행 전 검증/수정 |
| `PostToolUse` | 도구 실행 후 | 결과 처리/로깅 |
| `Notification` | 알림 발생 시 | 알림 커스터마이징 |

## 💻 응답 형식

### Continue (계속 실행)

```json
{
  "action": "continue"
}
```

### Block (실행 차단)

```json
{
  "action": "block",
  "message": "차단 사유"
}
```

### Modify (입력 수정)

```json
{
  "action": "modify",
  "tool_input": {
    "modified": "value"
  }
}
```

## 📎 예시 코드

```python
def process_hook(event_data):
    tool_name = event_data.get("tool_name")

    # 위험한 명령어 차단
    if "rm -rf" in str(event_data.get("tool_input", {})):
        return {
            "action": "block",
            "message": "위험한 명령어가 감지되었습니다"
        }

    return {"action": "continue"}
```
